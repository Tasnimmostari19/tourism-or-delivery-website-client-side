import React from 'react';

const Plan = ({ plan }) => {
    const { name, img, description } = plan;

    return (
        <div>
            <h2>this is plan</h2>

        </div>
    );
};

export default Plan;